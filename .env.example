# Copy this file to .env and fill in your values before running docker compose up

# PostgreSQL password for the freshrss database user
FRESHRSS_DB_PASSWORD=change_me_strong_password

# Timezone (used by FreshRSS cron for feed refresh times)
TZ=Europe/Berlin
